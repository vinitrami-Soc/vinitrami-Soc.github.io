# Pins are audited with `pip-audit -r requirements.txt` (make audit).
# Last audit: clean against the advisory database at the time of commit.

# --- API ---
fastapi==0.141.1
starlette==1.6.0           # pinned explicitly: several 2026 advisories below 1.3.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32   # multipart parser advisories fixed in 0.0.31+
pydantic==2.13.5
pydantic-settings==2.15.0

# --- HTTP / providers ---
httpx==0.28.1

# --- storage ---
SQLAlchemy[asyncio]==2.0.54
aiosqlite==0.22.1
asyncpg==0.30.0
redis==8.1.0

# --- background work ---
celery==5.6.3

# --- offline datasets ---
geoip2==5.3.0
